name: Test

on:
  pull_request:
    paths:
      - "./**"
  workflow_dispatch:

defaults:
  run:
    working-directory: ./

jobs:
  terraform:
    name: Terraform
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      contents: read

    steps:
      - name: Update Pull Request
        uses: actions/github-script@v6
        if: github.event_name == 'pull_request'
        with:
          script: console.log("Trying GitHub actions!")
